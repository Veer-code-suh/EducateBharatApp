import { Dimensions } from 'react-native';
const windowWidth = Dimensions.get('window').width;
const windowHeight = Dimensions.get('window').height;
const COLOR = {
    col1: "white",
    col3: "#5C469C",
    col2: "#5C469C",
    col4: "#D6C7FF",
}

export {COLOR,windowHeight,windowWidth}